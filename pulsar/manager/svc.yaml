# pulsar-manager-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: pulsar-manager
  namespace: pulsar
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
  selector:
    app: pulsar-manager
